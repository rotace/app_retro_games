[package]
name = "runner-debian"
version = "0.1.0"
edition = "2021"

[dependencies]
retro_core = { path = "../retro_core" }
libc = "0.2"
clap = { version = "4.0", features = ["derive"] }
thiserror = "1.0"
